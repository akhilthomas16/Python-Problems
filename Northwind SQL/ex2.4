SELECT city, 
       COUNT(DISTINCT employee_id) AS EmployeeCount, 
       COUNT(DISTINCT customer_id) AS CustomerCount
FROM (
    SELECT CAST(employee_id AS TEXT) AS employee_id, city FROM employees
    UNION ALL
    SELECT CAST(customer_id AS TEXT) AS employee_id, city FROM customers
) AS Combined
GROUP BY city
ORDER BY city;

#not working
